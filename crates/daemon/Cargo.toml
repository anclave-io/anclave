[package]
name = "anclaved"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
anclave-protocol = { path = "../protocol" }
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread", "signal", "io-util"] }
